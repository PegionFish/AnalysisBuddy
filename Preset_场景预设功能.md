# AnalysisBuddy 场景预设（Preset）功能执行方案

> 本方案供执行 Session 直接调度。执行者为具备子代理编排能力的 AI 主代理，应**最大化并行**：按下方波次（Wave）调度，同一波次内各子代理互不冲突、可同时开工。
>
> **交接说明**：本方案经确认后即为最终执行依据，规划方不再介入；执行 Session 全权负责实施、验证与评审，遇与代码现状不符处可在不违背「功能设计」与「工程纪律」的前提下自行微调并在任务板留痕。执行前先 `git pull` 同步最新源码并确认工作树干净。

## 〇、执行总则（多子代理并行规则）

1. **角色分工**：主代理只做编排（拆任务、分波次、验收集成），禁止亲自写码。子代理分四类：`Coding`（实现）、`Verify`（测试/lint/构建取证）、`CodeReview`（评审）、`Research`（仅在遇阻时补查）。
2. **隔离原则**：同一波次内每个子代理负责**互不重叠的文件集合**（见各任务「文件清单」）；两个任务若触碰同一文件必须串行到不同波次。
3. **契约先行、逐字下发**：第一阶段产出「契约冻结包」（schema 字段定义 + Rust 类型签名 + TS 类型签名 + IPC 命令签名），主代理将其**逐字复制**进每个后续子代理的 prompt，禁止子代理各自假设数据结构——契约不一致是本计划头号风险。
4. **每任务自带验证**：Coding 子代理完成后主代理立即派 Verify（`cargo test` 相关 crate / `npx vitest run` / `tsc --noEmit` / `eslint`），失败即回原 Coding 修复，不带病进下一波次。
5. **完成门**：每波次全部 Verify 通过后才进入下一波次；最终波次执行 Ultra Review（3 个 CodeReview 并行，分别只看完整性/正确性/影响面）。
6. **工程纪律（子代理 prompt 必须包含）**：i18n 键 zh/en 同批（`node scripts/check-i18n.mjs` 门禁）；UI 组件一律经 `ui/src/ipc/ipc.ts` 门面，禁止直接 import `ipc/real.ts`；文案不硬编码进源码；不新增/不重排 plugin-validator 的 25 条冻结规则；不动 protocol-v1 线协议消息。

## 一、功能设计（通用机制，不预设任何具体场景）

预设是**与场景无关的通用机制**：任何插件可为任意测试场景声明预设，预设仅描述「一组该场景重点关注的指标如何从日志指标中识别」，场景语义完全开放、由插件作者自行命名，核心不做任何内建场景假设。

### 数据模型（插件预设与用户预设同构）

```jsonc
// plugin.json 新增可选顶层字段（schema additionalProperties:true，旧插件零影响）
"presets": [
  {
    "id": "^[a-z0-9][a-z0-9-_]{0,63}$",
    "name": { "zh": "…", "en": "…" },          // 强制双语
    "description": { "zh": "…", "en": "…" },   // 可选
    "entries": [                                // 顶层条目：对所有分组生效
      { "want": "语义槽id(可选,同want仅首个命中生效)", "names": ["候选1", "候选2"] }
    ],
    "groups": [                                 // 可选标签分组（平台/供应商/任意分类，核心不解释语义）
      { "id": "…", "name": {"zh":"…","en":"…"}, "entries": [ …同上… ] }
    ],
    "keywords": ["模糊兜底关键词(仅穷举整体零命中时启用)"]
  }
]
```

- `names` 候选项 = 规范化 metric_id 或原始指标名，**穷举式精确匹配**（工具条目列表已知时可硬性遍历全部候选）。
- 预设只存 `plugin_id + metric 键`；UI 选择态是复合 id `file_id:plugin_id:metric_id`（file_id 会话级），应用时按当前 metricTree 逐文件解析。
- 匹配三级：① 穷举精确（metric_id 精确 → name 大小写不敏感，同 want 首个命中，记录命中分组）→ ② keywords 子串模糊兜底（标注 matched_by=fuzzy）→ ③ 零命中：不改动当前选择，清单化提示 unmatched。
- 上限防膨胀：单插件预设 ≤32、单预设条目 ≤1000；非法预设仅丢弃该预设+诊断，不拒插件。
- 用户保存预设：从 selectedMetrics 反推 `plugin_id + metric_id` 存入 names（天然精确、无模糊项）。
- v1 不含图表视图/过滤器，但顶层结构保留未来追加可选段的兼容空间。

### 集成要点（均已按现行代码核验）

- 插件预设透出**零新增命令**：复刻 `PluginInfoDto` 的 author/changelog 可选透传范式（core/ab-app/src/commands/mod.rs L116-150），`list_plugins` 顺带返回。
- 用户预设 **3 个新 Tauri 命令**：`list_user_presets` / `save_user_preset` / `delete_user_preset`，存 `%APPDATA%\AnalysisBuddy\presets\<id>.abpreset.json`（tmp+rename 原子写、UTF-8 无 BOM、损坏回落空集，照抄 `.ab-modules.json` 模式；文件名由 id 派生、防路径穿越）。命令注册四处同步：lib.rs generate_handler!、build.rs REGISTERED_COMMANDS、capabilities/default.json、permissions/autogenerated（构建重生成）。
- 匹配在前端纯函数 `ui/src/lib/presetMatch.ts`（可单测）；应用走新增 `presets/apply` reducer（单次 dispatch 原子替换 selectedMetrics，空命中不应用，保持 P1-04 语义）。预设 UI 状态放组件本地 useState，不进 SessionContext。
- 宿主校验：ab-host validate() 追加 validate_presets（上限/去重/形状），失败降级不拒插件；plugin-validator 本批零规则改动（单源纪律自动跟随 schema），MAN-14 留作后续。

## 二、波次与任务分配

### Wave 0：契约冻结（主代理亲自完成或单子代理）
产出「契约冻结包」：JSON Schema `presets` 定义 + `PresetDef/PresetEntry/PresetGroup` Rust 类型签名 + 对应 TS 类型 + 3 个 IPC 命令签名 + presetMatch 函数签名。所有后续子代理 prompt 内嵌此包逐字内容。

### Wave 1：规范与 Rust 契约层（3 子代理并行）

| 子代理 | 任务 | 文件清单（独占） |
|---|---|---|
| C1 Doc | CCP 提案 + schema/spec/示例/开发者指南 | docs/developer-guide/proposals/CCP-*.md、docs/spec/plugin-manifest.schema.json、docs/spec/protocol-v1.md §7.2、docs/spec/examples/manifest-ok-presets.json、docs/developer-guide/04-manifest-reference.md、02-write-a-plugin.md |
| C2 Protocol | ab-protocol 类型 + serde 往返用例 | core/ab-protocol/src/manifest.rs（追加 Option 字段+新类型）、其 serde 测试 |
| C3 Validator-fixtures | validator 正例 fixtures（验证 MAN-01 自动放行，不改规则代码） | tools/plugin-validator/fixtures/（仅加正例）、tests/rules_manifest.rs 追加用例 |

依赖：C2 依赖 Wave 0 契约包；C1/C3 可与 C2 并行（C1 的 schema 文本 = 契约包内容）。

### Wave 2：宿主校验 + 用户预设存储（2 子代理并行）

| 子代理 | 任务 | 文件清单（独占） |
|---|---|---|
| C4 Host | ab-host validate_presets（上限/去重/降级语义）+ 单测 | core/ab-host/src/manifest.rs、其 tests |
| C5 Store | commands/presets.rs 三命令 + 原子存储 + 注册四处同步 + 集成测试 | core/ab-app/src/commands/presets.rs、lib.rs、build.rs、capabilities/default.json、permissions/autogenerated/、core/ab-app/tests/presets_*_test.rs |

C4/C5 文件无交集可并行；C5 复用 C2 类型（Wave 1 已完成）。Verify：`cargo test -p ab-host`、`cargo test -p ab-app`、capabilities 三方交叉测试。

### Wave 3：UI 层（4 子代理并行，mock 轨先行）

| 子代理 | 任务 | 文件清单（独占） |
|---|---|---|
| C6 Ipc | IPC 四处同步：types.ts/ipc.ts/mock.ts/real.ts（PluginInfo.presets + 3 命令，mock 用 localStorage 槽位 + list_plugins fixture 加带 presets 插件） | ui/src/ipc/* 四文件 |
| C7 Match | presetMatch.ts 纯函数 + 全分支单测 | ui/src/lib/presetMatch.ts、其 test |
| C8 State | session.ts 新增 `presets/apply` reducer + applyPreset/savePresetAs actions + reducer 单测 | ui/src/state/session.ts、其 test |
| C9 I18n | `presets.*` 双语键树（zh/en 同批） | ui/src/i18n/zh.json、en.json |

C6/C7/C8/C9 文件完全不相交；C8 依赖 C7 的函数签名（契约包已给定，无需等实现）；组件层在 Wave 4。Verify：`tsc --noEmit`、`eslint .`、`npx vitest run`、`check-i18n.mjs`。

### Wave 4：组件 + 透传接线（2 子代理并行）

| 子代理 | 任务 | 文件清单（独占） |
|---|---|---|
| C10 UI | PresetBar.tsx（下拉/应用/保存命名/删除/结果 toast，来源标记，本地 state）+ 组件测试；遵循 role 按钮规避原生 checkbox 的既有约定 | ui/src/components/PresetBar.tsx/.css/.test.tsx、MetricTree 挂载点最小改动 |
| C11 DTO | PluginInfoDto 追加 presets 透传 + 组装点同步 + DTO 测试 | core/ab-app/src/commands/mod.rs、plugin.rs、plugin_manager.rs 组装点、其 tests |

Verify：全量 `cargo test` + `npx vitest run` + `cargo build`。

### Wave 5：示例插件 + 集成验收 + Ultra Review

- C12 Example：builtin-csv 与 demo-tool 的 plugin.json 各加 1-2 个示范预设（内容自定，勿内建任何特定场景假设，作为「如何写预设」的样板）。
- Verify 集成：mock 轨应用预设→selectedMetrics 断言；cargo 全测；lint 全绿。
- Ultra Review：3 个 CodeReview 子代理并行，各自只看一个维度——完整性（需求全覆盖）/ 正确性（逻辑与安全）/ 影响面（回归与破坏性）；主代理合并去重按严重度出报告，发现问题回派对应 Coding 修复后重新 Verify。

### 任务依赖图

```
Wave0(契约包) ─┬→ C1 Doc ──────────────┐
               ├→ C2 Protocol ─┬→ C4 Host ─┐
               │               └→ C5 Store ─┼→ C11 DTO ─┐
               └→ C3 Fixtures ─────────────┘           ├→ Wave5(C12+Verify+Review)
   Wave3: C6 Ipc ∥ C7 Match ∥ C8 State ∥ C9 I18n ─→ C10 UI ─┘
```

## 三、验收标准（DoD）

1. 旧插件/旧 manifest 全部照常通过校验与加载（零破坏）；plugin-validator 25 条规则零改动。
2. 带 presets 的插件经 list_plugins 透出预设；用户预设 CRUD 往返 + 损坏文件回落正确。
3. 三级匹配全分支有单测（穷举/分组/want 去重/模糊兜底/零命中不改选择）。
4. 应用预设为单次 dispatch 原子替换；空命中不改动现有选择。
5. `cargo test --workspace`、独立 crate（builtin-csv/plugin-validator）测试、`tsc --noEmit`、`eslint`、`vitest`、`check-i18n.mjs` 全绿。
6. 新 Tauri 命令经 capabilities 三方交叉测试；UI 无直接 import real.ts。

## 四、风险与缓解

| 风险 | 缓解 |
|---|---|
| 并行子代理数据结构漂移 | Wave 0 契约冻结包逐字下发；集成期 DTO/类型不一致即回派修复 |
| 同文件并发编辑冲突 | 波次内文件清单独占制；跨波次严格门控 |
| 契约五处漂移（spec/protocol/ab-protocol/指南/validator） | C1 单点负责文档侧同批落地；validator 靠单源纪律自动跟随 |
| 穷举表失控 | 32/1000 双上限，超限丢弃该预设不拒插件 |
| 复合 id 含 file_id 不可移植 | 预设只存 plugin_id+metric 键，应用时现解析 |
| 单 Context 重渲染放大 | 预设 UI 本地 state + 单次 dispatch |
| 新控件破坏既有 e2e 选择器 | 遵循 MetricTree 既有规避约定 |
| mock/real 双轨断裂 | 一律经 ipc.ts 门面，mock 补桩与 fixture |

## 五、Rejected Alternatives

1. 独立 presets.json 指针文件：manifest 本就全量解析，指针+惰性缓存徒增复杂度；上限约束下内联足够。
2. 后端匹配引擎 + match_preset 命令：前端已持有 metricTree，纯函数匹配更易测试，省去后端索引缓存生命周期。
3. 用户预设存 localStorage：与 .absession/.ab-modules.json 文件化惯例不一致，且不随便携目录迁移。
4. 本批追加 MAN-14 冻结规则：四步流程成本高，宿主降级校验已兜底；留作后续跟进。
5. 内建具体场景（如特定压测场景）：预设必须是场景无关的通用机制，场景语义由插件作者开放定义。